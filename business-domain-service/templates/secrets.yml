apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-env
  labels:
     {{- include "..labels" . | nindent 4 }}
data:
  SERVICE_PORT: {{ .Values.app_env.servicePort}}
  ACCOUNT_API_ENDPOINT: {{ .Values.app_env.endPoint }}


